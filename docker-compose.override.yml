# Override file for local development customization
# This file is automatically used by docker-compose and is typically added to .gitignore
# Copy this file's contents to create your own docker-compose.override.yml

version: '3.8'

# Example overrides:
# services:
#   api:
#     environment:
#       LOG_LEVEL: debug
#     ports:
#       - "3001:3000"
